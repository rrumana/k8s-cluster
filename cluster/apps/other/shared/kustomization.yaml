apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: other

resources:
  - externalsecret.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: other-shared
      app.kubernetes.io/managed-by: argocd
    includeSelectors: true
