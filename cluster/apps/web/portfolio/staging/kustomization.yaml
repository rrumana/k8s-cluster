apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: web

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - pdb.yaml
  - networkpolicy.yaml
  - vpa.yaml

commonLabels:
  app.kubernetes.io/name: portfolio-staging
  app.kubernetes.io/managed-by: argocd
