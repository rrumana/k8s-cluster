apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: web

resources:
  - externalsecret.yaml

commonLabels:
  app.kubernetes.io/part-of: web-shared
  app.kubernetes.io/managed-by: argocd
