apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai

resources:
  - externalsecret.yaml

commonLabels:
  app.kubernetes.io/part-of: ai-shared
  app.kubernetes.io/managed-by: argocd
