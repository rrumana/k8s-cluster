# platform/gitops/argocd/apps/volsync-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: volsync
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default

  sources:
    - repoURL: harbor.rcrumana.xyz/thirdparty-charts
      chart: volsync
      # renovate: datasource=helm depName=volsync versioning=helm registryUrl=https://backube.github.io/helm-charts/
      targetRevision: "0.16.0"
      helm:
        releaseName: volsync
        valueFiles:
          - $values/cluster/platform/base/backup/volsync/values.yaml
    - repoURL: https://github.com/rrumana/k8s-homelab-2.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: backup

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
