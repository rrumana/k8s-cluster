apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-ai-daily
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  schedule: "0 30 3 * * *"
  # Temporary NAS-removal pause. Remove to resume scheduled base backups.
  suspend: true
  backupOwnerReference: cluster
  cluster:
    name: pg-ai
  immediate: true
