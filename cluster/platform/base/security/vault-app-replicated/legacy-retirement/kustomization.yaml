apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The canonical runtime objects below were adopted in place from the retired
# Helm release and remain permanent dependencies of vault-app-replicated.
resources:
  - shared-resources.yaml
