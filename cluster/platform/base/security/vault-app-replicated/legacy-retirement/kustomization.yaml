apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Staged intentionally. Add this directory to the parent kustomization only
# after the legacy Vault Application has reconciled with self-heal and prune
# disabled. See ../RUNBOOK.md.
resources:
  - shared-resources.yaml
  - cleanup-rbac.yaml
  - cleanup-job.yaml
